name: Build & Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install PyMuPDF python-docx Pillow PySide6 pyinstaller

      - name: Build EXE
        run: pyinstaller pdf2word.spec --noconfirm --clean

      - name: Install NSIS
        run: choco install nsis -y --no-progress

      - name: Build NSIS installer
        run: |
          & "C:\Program Files (x86)\NSIS\makensis.exe" scripts/installer.nsi

      - name: Upload EXE (portable)
        uses: softprops/action-gh-release@v2
        with:
          files: dist/pdf2word.exe

      - name: Upload installer
        uses: softprops/action-gh-release@v2
        with:
          files: dist/pdf2word-setup.exe

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install PyMuPDF python-docx Pillow PySide6 pyinstaller

      - name: Build app
        run: pyinstaller pdf2word.spec --noconfirm --clean

      - name: Cleanup build cache
        run: rm -rf build pdf2word.spec.bak

      - name: Create DMG
        run: |
          mkdir -p dist/dmg
          cp -R dist/pdf2word dist/dmg/
          ln -sf /Applications dist/dmg/Applications
          hdiutil create -volname "PDF转WORD" \
            -srcfolder dist/dmg \
            -ov -format UDZO \
            -size 200m \
            dist/PDF转WORD转换器.dmg

      - name: Upload DMG
        uses: softprops/action-gh-release@v2
        with:
          files: dist/PDF转WORD转换器.dmg
